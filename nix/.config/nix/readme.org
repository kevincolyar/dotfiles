* Setup

** Determinate Nix Installer

#+begin_src sh
curl --proto '=https' --tlsv1.2 -sSf -L https://install.determinate.systems/nix | \
  sh -s -- install
#+end_src

** Home Manager 

Install

#+begin_src sh
  nix run home-manager/master -- init --switch
#+end_src

** Linux

#+begin_src sh
    home-manager switch --flake ~/.dotfiles/nix/.config/nix#$HOST
#+end_src

** Darwin

Bootstrap nix-darwin

#+begin_src sh
    sudo nix run nix-darwin/master#darwin-rebuild -- switch --flake ~/.config/nix#$HOST
#+end_src

Update System

#+begin_src sh
    sudo darwin-rebuild switch --flake ~/.dotfiles/nix/.config/nix#$HOST
#+end_src

* Update Channels

#+begin_src sh
    nix flake update --flake ~/.dotfiles/nix/.config/nix
#+end_src

* Collect Garbage

#+begin_src sh
  nix-collect-garbage
#+end_src
